# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

default:
  owner: one (Account)
  processor: stripe
  processor_id:
  default: true

other:
  owner: one (Account)
  processor: braintree
  processor_id:
  default: false
