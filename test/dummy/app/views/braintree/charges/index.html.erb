<div class="d-flex justify-content-between">
  <h1>Braintree Charges</h1>

  <div>
    <%= link_to "New Braintree Charge", new_braintree_charge_path, class: "btn btn-primary" %>
  </div>
</div>

<% @charges.each do |charge| %>
  <div>
    <%= link_to "Pay::Charge #{charge.id}", braintree_charge_path(charge) %>
  </div>
<% end %>
