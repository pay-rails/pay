<div class="d-flex justify-content-between">
  <h1>Paddle Classic Charges</h1>

  <div>
    <%= link_to "New Paddle Classic Charge", new_paddle_classic_charge_path, class: "btn btn-primary" %>
  </div>
</div>

<% @charges.each do |charge| %>
  <div>
    <%= link_to "Pay::Charge #{charge.id}", paddle_classic_charge_path(charge) %>
  </div>
<% end %>
