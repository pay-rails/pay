language: ruby
cache: bundler
rvm:
  - 2.5.7
  - 2.6.5
  - 2.7.0
before_install:
  - gem install bundler
install:
  bundle install
script:
  - bundle exec rake db:test:prepare
  - bundle exec rake test
