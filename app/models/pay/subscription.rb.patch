# Patch for app/models/pay/subscription.rb
# Add pay_the_fly to the processor helpers loop
#
# Change:
#   %w[braintree stripe paddle_billing paddle_classic lemon_squeezy fake_processor].each do |processor_name|
# To:
#   %w[braintree stripe paddle_billing paddle_classic lemon_squeezy pay_the_fly fake_processor].each do |processor_name|
